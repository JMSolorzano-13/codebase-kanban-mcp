# Agents

### @planner — spec/closure. Discovery interviews, Given/When/Then specs, approves @architect designs, validates completions, evolves constitution.md. Success: all specs user-approved before architecture starts. See prompts/prompt-planner.agent.
Learned Capabilities: spec-001/002 closed with no constitution diff. spec-003 first evolution: IV.1–IV.4 + IX.2. spec-004: MODIFIED IX.2 + ADDED IX.5 (fence, user-triggered fill, skip `.sdd-skill`) confirmed at close. spec-005: MODIFIED IX.2 (in-place Specs expand / enrich-all / dual matcher / Todo pending-only; Archive not this spec) confirmed at close. spec-006: MODIFIED IX.2 (CBM Specs-tab archive; GET merge + POST flag object; session Show archived; SDD-ADR-029..033) confirmed at close. spec-007: MODIFIED IX.2 (last-indexed display TZ; runtime-local Intl; dateTime/title + store ISO Z; SDD-ADR-034) confirmed at close. spec-008: MODIFIED IX.2 (Mixed Todo grill epics; additive epics[] cap 64; kind only on epics; letter E --color-epic-mark; Todo epics-then-specs; omit-until sdd; SDD-ADR-035..038) confirmed at close. spec-009: MODIFIED IX.2 (Specs-tab presence sdd OR grill; host Kanban same OR; notSddSkill last-resort; omit-until-true + restore inbound tab=specs; spec-008 omit-until-sdd superseded; SDD-ADR-039..041) confirmed at close. spec-010: MODIFIED IX.2 (Game tab silent win; GET /api/game-board one-shot; Game wins over Specs; dedicated GameBoardTab; leftover tab=specs + gamedev → game; SDD-ADR-042..045) confirmed at close. spec-011: MODIFIED IX.2 (Game phase board; filled arrays + Inbox conversion; spec-010 empty arrays superseded for paint; SDD-ADR-046..051) confirmed at close. spec-012: MODIFIED IX.2 (Game expand + archive + deps; dedicated game_archive; POST /api/game-board; spec-011 no-expand/archive/POST superseded; drag stays forbidden; SDD-ADR-052..057) confirmed at close. spec-013: MODIFIED IX.5 (XOR trio: `.gamedev/` dir wins; no sdd merge/fallback; `.gamedev` not ALWAYS_SKIP) + MODIFIED IX.2 append (SDD-ADR-058..061) confirmed at close. spec-014: MODIFIED IX.2 (Game visibility filters; Show Dones default hide; Track A/B/All exclusive All includes H; archived done AND; client React state; Specs lock; SDD-ADR-062..064) confirmed at close. spec-015: MODIFIED IX.2 (Specs debt chrome + EpicCard id wrap; always-emit debt[{id,title}] cap 16; heading Status wins; Specs-only strip; EpicCard wrap; SDD-ADR-065..068) confirmed at close. spec-016: MODIFIED IX.2 (Game Inbox registry hide + InboxCard wrap; regular file sole hide skip Companion-to/roadmap; absent/dir keeps spec-011; parse last-wins NNN exact slug; Inbox wrap; Artifact truncate locked; Specs does not read the registry; SDD-ADR-069..071) confirmed at close. spec-017: MODIFIED IX.2 (Game debt chrome; always-emit debt[{id,title}] cap 16; parse backlog.md in game_board.c; Game-only strip after BlockedStrip; reuse specBoard.openTechDebt; parseGameBoard keeps debt; Specs stays TECH_DEBT.md; never create backlog.md; SDD-ADR-072..074) confirmed at close. spec-002: Gherkin `columns` illustration ≠ live `{sdd_skill_present,specs}` — tests mock C JSON.

### @architect — plan. Full technical architecture+stack+versions+DB schema+API contracts+WBS → plan.md/tasks.md/checklist.md, ADRs, initial constitution.md. Success: @implementer works autonomously from plan.md/tasks.md. See prompts/prompt-architect.agent.
Learned Capabilities: graph-ui-only WBS works when C already exposes list_projects + indexed_at + path-derived name; lock graph hex in tests instead of screenshots.

### @implementer — code. Production code, constitution.md strict, >80% coverage, never revert (Fix Forward only), @sdd-* breadcrumbs, blocks on ambiguity. Success: passes @review without fixes. See prompts/prompt-implementer.agent.
Learned Capabilities: extract StatsTab pieces before delete; useProjects must not keep schema:null placeholders. Workspace: tab strip under header not inside it; Specs presence is one-shot spec-board, not the 4s Kanban poll. spec-003: one `cbm_identity_admit` for HTTP+MCP+jobs. spec-004: fill after `saved_adr` capture via `adr_fill` flag (not incremental-vs-full); `.sdd-skill` ALWAYS_SKIP; POST `/api/adr` 32768. spec-005: enrich every listed spec + dual done matcher in `spec_board.c`; `expandedIds` Set keyed by spec id; Todo pending-only. spec-006: `spec_archive` UPSERT in project `.db`; GET merge after skill read; POST `/api/spec-board` returns flag object; `await refresh()` after 200; `publish_staged` copies flags. spec-007: drop `timeZone` on `formatIndexedAt`; Intl localFmt vs utcFmt oracles; surfaces already call the helper (test-only Task #2). spec-008: grill walk in `spec_board.c` (HTTP no grill fopen); additive `grill_skill_present` + `epics[]` cap 64; POST epic-id 404; EpicCard display-only; Todo epics-then-specs; `--color-epic-mark` #7d8ec9. spec-009: `useSddSkillPresent.bodyHasSkill` is 200 AND (sdd OR grill); SpecBoardTab host same OR; App restore inbound `tab=specs`; graph-ui only. spec-010: dedicated `cbm_game_board_t` + GET `/api/game-board`; `useGameBoard` one-shot; `showGame` settled AND present; `showSpecs` AND NOT Game; `GameBoardTab` not `SpecBoardTab`; `resolveWorkspaceTab` leftover specs+gamedev → game; fopen rb only. spec-011: widen card + exist-only walk; `game_grill_*` local (not spec_board extract); convert Companion-to exact OR slug+table NNN; grow to_json; compiled owner/track; card `writeText` + select-text no toast; chrome continue stays `<p>`. spec-012: dedicated `game_archive` sibling of `spec_archive`; local `## What it does` extract; C blocked overlay before JSON; GET merge matching ids; POST flag object; `refresh()` never unset settled/present; title Set; Archive expanded done only. spec-013: `adr_select_trio` XOR; local `cbm_is_dir` (no `spec_board.h`); empty `.gamedev/` still marks; `.gamedev` not ALWAYS_SKIP; AdrTab stays generic. spec-014: visiblePhaseCards(showArchived, showDones, track); chrome | Show Dones + Track trio; lastProjectRef reset; Specs lock no copy. spec-015: parse TECH_DEBT.md in spec_board.c (HTTP no debt fopen); always-emit debt[]; Specs strip role=region; EpicCard id break-all. spec-016: parse epics_registry in game_board.c (HTTP no registry fopen); present=regular file skip load_conv; hide-set in_progress/closed/parked last-wins NNN exact slug; InboxCard id break-all; Artifact truncate locked. spec-017: parse backlog.md in game_board.c (HTTP/spec_board no backlog fopen; no spec_board helper); always-emit debt[]; Game strip after BlockedStrip; parseGameBoard keeps debt; never create backlog.md.

### @review — audit. Static audit: constitution 100%, OWASP, SOLID, type safety. Surgical feedback, no fixing. Approve/reject only, no in-between. Success: 80% first-pass rate. See prompts/prompt-review.agent.
Learned Capabilities: none yet

### @tester — validate. DEVELOPMENT(fast)+CERTIFICATION(rigorous) levels. Attempt counter max 2 before @sentinel. Success: catches 95% of bugs pre-production. See prompts/prompt-tester.agent.
Learned Capabilities: constitution IX.4 — DEV Vitest is enough for this UI spec; coverage reporter not installed so >80% stays unmeasured.

### @sentinel — pivot. Activates at 2 consecutive failures. REFACTOR|REPLACE|RETHINK, logs to ADR+decisions.log, resets counter. Success: unblocks in <1h. See prompts/prompt-sentinel.agent.
Learned Capabilities: none yet

### @human-trainer — document+gate. Task/spec summaries, PROJECT-OVERVIEW.md, QUICK-DEBUG.md, ARCHITECTURE-VISUAL.mmd, breadcrumbs, blocks closure on human confirmation. Success: new dev understands project in 15min. See prompts/prompt-human-trainer.agent.
Learned Capabilities: spec-003 closeprep raised CONSTITUTION RECOMMENDATION (IV.1 stale vs admit). spec-004 closeprep raised IX fence/gate; Trigger B waits on close phrase before @planner. spec-005 closeprep raised MODIFIED IX.2 (expand contract); Archive left to grill epic-002. spec-006 closeprep raised MODIFIED IX.2 (archive contract); constitution gate before close. spec-007 closeprep raised MODIFIED IX.2 (display TZ / SDD-ADR-034); IV.4 already freshness-field, silent on TZ. spec-008 closeprep raised MODIFIED IX.2 (Mixed Todo / additive epics[] / E token / SDD-ADR-035..038); I.2 already zero-write; III.1 silent on E (locked in IX.2). spec-009 closeprep raised MODIFIED IX.2 (presence sdd OR grill; spec-008 omit-until-sdd superseded; SDD-ADR-039..041). spec-010 closeprep raised MODIFIED IX.2 (Game tab silent win; GET /api/game-board; spec-009 gamedev-only sentence incomplete; SDD-ADR-042..045). spec-011 closeprep raised MODIFIED IX.2 (filled Game arrays + Inbox conversion; spec-010 empty-array paint superseded; SDD-ADR-046..051). spec-012 closeprep raised MODIFIED IX.2 (expand+archive+POST; spec-011 no-expand/archive/POST superseded; drag stays; SDD-ADR-052..057). spec-013 closeprep raised MODIFIED IX.5 (XOR trio) + APPEND IX.2 (SDD-ADR-058..061). spec-014 closeprep raised MODIFIED IX.2 (visibility filters / SDD-ADR-062..064). spec-015 closeprep raised MODIFIED IX.2 (debt chrome + EpicCard wrap / SDD-ADR-065..068). spec-016 closeprep raised MODIFIED IX.2 (Game Inbox registry hide + InboxCard wrap / SDD-ADR-069..071). spec-017 closeprep raised MODIFIED IX.2 (Game debt chrome / SDD-ADR-072..074).

[HANDOFF] @planner→@architect→@implementer→@human-trainer→@review→[fail→@implementer|fail²→@sentinel|pass→@human-trainer(spec summary)]→@planner(close)
